# Plan — <project>

> **Misión finita:** qué debe quedar resuelto cuando este plan termine. Un plan que no termina nunca no es un plan.
> **Contexto del proyecto:** `context_<project>.md`

## Estado

Última actualización: `<fecha>`.

Resumen breve de dónde está el proyecto ahora mismo.

## Fases / pasos

1. `<paso>` — estado: pendiente / en curso / hecho.
2. `<paso>` — estado: pendiente / en curso / hecho.

## Gates

- `<decisión o verificación que debe pasar antes de continuar>`.

## Siguiente tarea

`<la única tarea concreta que sigue, no una lista larga>`.

## Criterios de finalización

Este plan termina cuando:
- `<criterio>`;
- `<criterio>`.

Si el proyecto continúa con otro objetivo, se crea un plan nuevo en vez de extender este.
