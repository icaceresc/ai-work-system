# Context — <project>

> **Propósito:** mapa durable del proyecto. No duplica detalle técnico; referencia a sus artifacts propietarios.
> **Puntero al plan activo:** `plan_<project>.md`

## 1. Propósito

Qué problema resuelve este proyecto y para quién. 2-4 líneas.

## 2. Arquitectura / owners

- `<artifact o sistema>` — dueño de `<qué información>`.
- `<artifact o sistema>` — dueño de `<qué información>`.

## 3. Estado durable

Qué es cierto sobre el proyecto independientemente del plan activo (no estado de ejecución del día a día — eso vive en el plan).

## 4. Decisiones adoptadas

- `<decisión>` — por qué se tomó, cuándo.

## 5. Límites

Qué queda explícitamente fuera de este proyecto.

## 6. Cómo reconstruir el proyecto

Si se pierde el contexto de una conversación:
1. leer este archivo;
2. leer el plan activo (`plan_<project>.md`);
3. recuperar detalle desde los artifacts propietarios listados en la sección 2;
4. no usar historial de chat como fuente de verdad.
